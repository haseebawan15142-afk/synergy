# Brand assets

| File | Use |
|------|-----|
| `scl-mark-source.png` | Original SCL mark (black background) |
| `scl-mark.png` | **Header/footer** — transparent BG, enhanced for web |
| `logo.svg` | Fallback wordmark-only (optional) |
| `logo.png` | Legacy full raster from synergy.net.pk |

Replace `scl-mark-source.png` when a higher-resolution or SVG export is available, then re-run transparency script in README or ask the agent to regenerate `scl-mark.png`.
